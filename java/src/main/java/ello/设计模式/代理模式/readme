a通过b访问c  --a不能直接访问c
为其他对象提供一种代理，以控制这个对象的访问
作用：1：控制访问 2：功能增强

1。静态代理（必须有接口）：a：代理类自己手工实现，创建一个代理java类  b：同时你要代理到目标类是明确到
    缺点：代理类多
2。动态代理：解决静态代理目标类很多；修改了接口中到方法时，不会影响代理类
    使用jdk反射机制，创建代理对象，并 动态 指定要代理的目标类

cglib动态代理（可以继承即可）：通过继承目标类，创建它的子类，在子类中重写父类方法，要去目标类不能是final，方法也不能是final(方法不能增强，不起作用)
动态生成字节码（运行期扩展）asm字节码框架 生成子类并调用父类的方法
原理：动态生成被代理类的子类，子类重写代理类方法，类不能被final修饰(无法继承)方法不能被修饰(无法被重写)，通过拦截父类方法实现方法增强，比jdk动态代理快
底层：修改字节码生成子类
spring bean拷贝

